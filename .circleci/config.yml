version: 2.1

jobs:
  test:
    docker:
      - image: circleci/ruby:2.6.3
    parallelism: 3
    environment:
      - RAILS_ENV: test
    steps:
      - checkout
      - run: bundle install
      - run: bundle exec rspec

workflows:
  version: 2
  test:
    jobs:
      - test

